import React from 'react';
import './App.css';

function App() {
  return (
    <div className="App">
      <header className="App-header">
       This is git practice and I really want to learn,this.
       But I don't know why does my git add command is not working, may be this is already added to the git repo.
       Now lets work on commit 1
       Now lets work on commit 2
       new branch nigga
      </header>
    </div>
  );
}

export default App;
